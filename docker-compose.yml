version: '3.8'
services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: discord-economy-bot
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
